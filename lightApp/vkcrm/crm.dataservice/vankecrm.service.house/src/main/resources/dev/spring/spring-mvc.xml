<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd   
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd">

    <context:component-scan base-package="bingo.vkcrm.service.house.v1.controller" />

    <mvc:interceptors>

        <mvc:interceptor>
            <!--ContentType拦截器-->
            <mvc:mapping path="/api/**"/>
            <bean class="bingo.vkcrm.service.interceptors.NoContentTypeInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <!--AccessToken校验拦截器-->
            <mvc:mapping path="/api/**"/>
            <bean class="bingo.vkcrm.service.interceptors.TokenAuthInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <!--访问日志记录拦截器-->
            <mvc:mapping path="/api/**"/>
            <bean class="bingo.vkcrm.service.interceptors.LogInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <mvc:annotation-driven conversion-service="convertService">
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter" />
        </mvc:message-converters>
    </mvc:annotation-driven>
    <bean id="convertService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
        <property name="converters">
            <list>
                <bean class="bingo.vkcrm.service.convert.DateConvert"/>
            </list>
        </property>
    </bean>
    <mvc:default-servlet-handler />

    <!-- 视图解析模版 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver"
            p:prefix="/" p:suffix=".jsp"
            p:viewClass="org.springframework.web.servlet.view.JstlView" />
</beans>
