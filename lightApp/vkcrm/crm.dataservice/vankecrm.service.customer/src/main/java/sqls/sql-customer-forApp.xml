<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment>对外接口sql</comment>

    <!-- 根据房屋code查询房屋id-->
    <entry key="sql.query.house.code">
        <![CDATA[
			SELECT 
				id AS houseId 
			FROM 
				main_house AS a 
			WHERE 
				a.`code` = #houseCode# 
			LIMIT 1
	    ]]>
    </entry>
    
    <!-- 新增客户-->
    <entry key="sql.insert.customer.app">
        <![CDATA[
			INSERT INTO 
				main_customer(id,`code`,fullName,customerType,certificateType,certificateId,createTime) 
			VALUES 
				(#id#,#code#,#name#,1,#certificateType#,#certificateId#,NOW());
	    ]]>
    </entry>

    

</properties>
