<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>导入网大学时sql</comment>
	
	<!-- 通过员工编码获得其它信息 -->
    <entry key="get.netstudy.byempcode">
    <![CDATA[
        select ei.company_id      companyId,
		       ei.company_name    companyName,
		       ei.department_id   deptmentId,
		       ei.department_name deptmentName,
		       ei.emp_name        empName,
		       ei.id			  empId
		  from employee_info ei
		 where ei.emp_code = #empCode#
    ]]>
    </entry>
    <!-- 判断是否是省公司管理员-->
    <entry key="is.province.manager">
    <![CDATA[
        select o.fullname
          from sec_organization o
         where o.id =
               (select su.org_id
                  from sec_user su
                 where su.id = #managerId#)
    ]]>
    </entry>
     <!-- 判断当年已经有该员工的数据-->
    <entry key="is.has.nowyear.data">
    <![CDATA[
         select count(1)
           from emp_study_static ess
          where ess.emp_code = #empCode#
            and to_char(ess.last_updated_date, 'yyyy') =
                substr(#endDate#, 0, 4)
    ]]>
    </entry>
     <!-- 删除当年已经有该员工的数据-->
    <entry key="del.nowyear.data">
    <![CDATA[
          delete from emp_study_static ess
		    where ess.emp_code = #empCode#
		      and to_char(ess.last_updated_date, 'yyyy') = substr(#endDate#, 0, 4)
    ]]>
    </entry>
</properties>