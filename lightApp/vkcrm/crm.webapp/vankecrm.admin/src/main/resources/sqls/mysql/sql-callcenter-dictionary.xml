<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment>数据字典sql</comment>

    <!-- 根据客户id查询车辆信息-->
    <entry key="sql.query.dictionary">
        <![CDATA[
		SELECT
            di.id, di.code, di.value, d.name AS typeName
        FROM
            dim_dictionary d,
            dim_dictionary_items di
        WHERE
            d.id = di.dictionaryId AND d.code = #dictionaryCode#
        ORDER BY di.sortNo
	    ]]>
    </entry>
</properties>
